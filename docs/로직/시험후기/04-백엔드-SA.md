# 04. 백엔드 — Server Actions

> 시험후기 모듈은 Edge Functions 없이 **Server Actions만으로 구성**.
> 12개 SA + 3개 읽기 전용 함수 + 1개 RPC + 콤보 추출 유틸리티.

---

## 아키텍처 개요

```
클라이언트 (React)
    │
    │ Server Actions (lib/actions/reviews.ts)
    ▼
Supabase Client (서버 측)
    │
    ├─ submissions 테이블 (CRUD)
    ├─ submission_questions 테이블 (CRD)
    ├─ submission_combos 테이블 (CR)
    └─ user_credits (RPC 호출만)
```

- **Edge Functions 없음**: 외부 AI API 호출이 필요 없으므로 SA만으로 충분
- **인증**: `getUser()` → Supabase Auth 세션 검증 (쿠키 기반)
- **RLS**: 모든 쿼리에 `user_id` 조건 → Supabase RLS가 이중 검증

---

## 쓰기 함수 (7개)

### createDraft

```typescript
async function createDraft(data: Step1FormData)
  → { id: number }
```

**로직**:

```
1. getUser() — 인증 확인
2. 기존 draft 조회 (status='draft', user_id)
3. 기존 draft DELETE (1개 유지 정책)
4. submissions INSERT:
   - user_id, exam_date, exam_difficulty, pre_exam_level, achieved_level
   - exam_purpose, study_methods[], prep_duration, attempt_count
   - perceived_difficulty, time_sufficiency, actual_duration
   - used_recommended_survey
   - survey_* 필드들 (추천 서베이 사용 시 NULL)
   - status='draft', step_completed=1
5. 반환: { id: newSubmission.id }
```

**추천 서베이 분기**:

```typescript
if (data.used_recommended_survey) {
  // 커스텀 서베이 필드 NULL 처리
  insertData.survey_occupation = null;
  insertData.survey_student = null;
  // ... 8개 필드 모두 NULL
} else {
  // 커스텀 서베이 값 저장
  insertData.survey_occupation = data.survey_occupation;
  // ... 사용자 입력값
}
```

### updateDraft

```typescript
async function updateDraft(submissionId: number, data: Step1FormData)
  → { id: number }
```

**로직**: `createDraft`와 동일한 필드 → UPDATE (INSERT 대신)

```
1. getUser()
2. submissions UPDATE WHERE id = submissionId AND user_id AND status = 'draft'
3. 반환: { id: submissionId }
```

### saveQuestions

```typescript
async function saveQuestions(data: { submission_id: number, questions: QuestionInput[] })
  → {}
```

**로직**:

```
1. getUser()
2. submission_questions DELETE WHERE submission_id (기존 질문 전체 삭제)
3. Q1 자기소개 자동 추가:
   {
     submission_id,
     question_number: 1,
     combo_type: null,      // 콤보에 속하지 않음
     topic: "자기소개",
     question_id: null,
     custom_question_text: null,
     is_not_remembered: false
   }
4. 14개 질문 + Q1 = 15개 INSERT
5. submissions UPDATE (step_completed = 2)
6. 반환: {}
```

### completeSubmission

```typescript
async function completeSubmission(data: {
  submission_id: number,
  one_line_review: string,
  tips?: string
})
  → { creditGranted: boolean, nextCreditDate?: string }
```

**로직** (가장 복잡한 SA):

```
1. getUser()

2. 원자적 상태 전환:
   submissions UPDATE SET
     status = 'complete',
     one_line_review = $1,
     tips = $2,
     submitted_at = NOW(),
     step_completed = 3
   WHERE id = $3
     AND user_id = $4
     AND status = 'draft'  ← 핵심: draft만 complete 허용
   .single()

   → 0건 UPDATE = 이미 complete (중복 방지)

3. 콤보 추출:
   submission_questions 조회 (submission_id)
   → extractCombos(questions)
   → submission_combos INSERT (0~5개)
   → 실패해도 후기는 유지 (try-catch)

4. 크레딧 보상 (25일 룰):
   ① credit_granted=true인 이전 후기 조회
   ② creditCount < 2 → 무조건 지급
   ③ creditCount >= 2 → 25일 경과 확인
   ④ 지급 시: RPC increment_script_credits (SECURITY DEFINER)
   ⑤ 성공 시: submissions UPDATE credit_granted = true

5. 반환: { creditGranted, nextCreditDate }
```

**에러 처리**:

```
콤보 추출 실패 → console.error + 후기는 유지
크레딧 RPC 실패 → console.error + credit_granted = false
→ 재시도 가능 (다음 후기 작성 시 기록 갱신)
```

### deleteSubmission

```typescript
async function deleteSubmission(submissionId: number)
  → {}
```

**로직**:

```
1. getUser()
2. submissions SELECT (id, status) WHERE id AND user_id
3. status === 'complete' → throw Error("완료된 후기는 삭제할 수 없습니다")
4. submissions DELETE WHERE id AND user_id AND status = 'draft'
5. 반환: {}
```

### updateGrade

```typescript
async function updateGrade(data: { submissionId: number, achievedLevel: string })
  → {}
```

**로직**:

```
1. getUser()
2. submissions UPDATE SET achieved_level = $1
   WHERE id = $2 AND user_id = $3
3. 반환: {}
```

### getDraft

```typescript
async function getDraft(submissionId: number)
  → Submission
```

**로직**: draft 조회 (이어쓰기 폼 복원용)

```
1. getUser()
2. submissions SELECT * WHERE id AND user_id AND status = 'draft'
3. 반환: Submission
```

---

## 읽기 함수 (8개)

### getMySubmissions

```typescript
async function getMySubmissions()
  → Submission[]
```

```
submissions SELECT *
  .eq("user_id", auth.uid())
  .order("created_at", { ascending: false })
```

### getSubmissionWithQuestions

```typescript
async function getSubmissionWithQuestions(submissionId: number)
  → SubmissionWithQuestions
```

```
submission_questions SELECT *, questions(*)
  .eq("submission_id", submissionId)
  .order("question_number")

→ Submission + 질문 배열 (questions JOIN 포함)
```

### getSubmissionsWithQuestionsBatch

```typescript
async function getSubmissionsWithQuestionsBatch(submissionIds: number[])
  → Record<number, SubmissionWithQuestions>
```

```
submission_questions SELECT *, questions(*)
  .in("submission_id", submissionIds)

→ submissionId별로 그루핑 (N+1 방지)
```

- 서버 사전로드에서 사용: 모든 완료 후기 상세를 한 번에 조회
- `queryClient.setQueryData()` → 클라이언트 캐시 직접 주입

### getDraftQuestions

```typescript
async function getDraftQuestions(submissionId: number)
  → SubmissionQuestion[]
```

```
submission_questions SELECT *
  .eq("submission_id", submissionId)
  .order("question_number")
```

- 이어쓰기 시 Step 2 질문 복원용

### getStatsAndFrequency

```typescript
async function getStatsAndFrequency()
  → { stats: { totalReviews, totalTopics, totalParticipants }, frequency: FrequencyItem[] }
```

```
Promise.all([
  submissions.select("id", { count: "exact" }).eq("status", "complete"),
  submissions.select("user_id").eq("status", "complete"),      // distinct 참여자
  submission_combos.select("combo_type, topic")                 // 빈도 원본
])

→ 빈도 집계: topic × combo_type → frequency count
→ 정렬: frequency 내림차순
```

### getFrequency

```typescript
async function getFrequency()
  → FrequencyItem[]
```

- `getStatsAndFrequency`에서 빈도 부분만 추출한 버전

### getQuestionFrequency

```typescript
async function getQuestionFrequency(data: { topic: string, comboTypes: string[] })
  → QuestionFrequencyItem[]
```

```
1. submission_combos SELECT question_ids
   .in("combo_type", comboTypes)
   .ilike("topic", `%${topic}%`)

2. question_ids 배열 flatten + 빈도 카운트

3. questions SELECT (id, question_short, question_english, answer_type, topic)
   .in("id", uniqueQuestionIds)

4. 빈도 내림차순 정렬

→ { questionId, questionShort, questionEnglish, answerType, topic, frequency }[]
```

### getPublicReviews

```typescript
async function getPublicReviews(data: { page: number, limit: number, levelFilter?: string })
  → { reviews: Submission[], total: number }
```

```
submissions SELECT *, count
  .eq("status", "complete")
  .eq("achieved_level", levelFilter)     // optional
  .order("submitted_at", { ascending: false })
  .range(offset, offset + limit - 1)
```

---

## RPC 함수

### increment_script_credits

```sql
CREATE OR REPLACE FUNCTION increment_script_credits(p_user_id UUID, p_amount INTEGER)
RETURNS VOID AS $$
BEGIN
  UPDATE user_credits
  SET script_credits = script_credits + p_amount
  WHERE user_id = p_user_id;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

- **SECURITY DEFINER**: 함수 소유자(postgres) 권한으로 실행 → RLS 우회
- 이유: user_credits 직접 UPDATE는 RLS로 차단됨
- 호출: `completeSubmission()` → 크레딧 보상 시

---

## 콤보 추출 유틸리티

> `lib/utils/combo-extractor.ts`

### extractCombos

```typescript
function extractCombos(questions: QuestionItem[]): ExtractedCombo[]
```

**COMBO_RANGES 정의**:

```typescript
const COMBO_RANGES = [
  { type: "general_1",  start: 2,  end: 4,  count: 3, excludeTopics: true },
  { type: "general_2",  start: 5,  end: 7,  count: 3, excludeTopics: true },
  { type: "general_3",  start: 8,  end: 10, count: 3, excludeTopics: true },
  { type: "roleplay",   start: 11, end: 13, count: 3, excludeTopics: false },
  { type: "advance",    start: 14, end: 15, count: 2, excludeTopics: true },
];
```

**EXCLUDED_TOPICS**:

```typescript
const EXCLUDED_TOPICS = ["공원", "카페", "하이킹"];
```

**isValidForCombo 검증**:

```typescript
function isValidForCombo(q: QuestionItem, excludeTopics: boolean): boolean {
  if (!q.question_id) return false;           // master question 필수
  if (q.is_not_remembered) return false;       // 기억해야 함
  if (q.custom_question_text) return false;    // 직접 입력 불가
  if (excludeTopics && EXCLUDED_TOPICS.includes(q.topic)) return false;
  return true;
}
```

**추출 과정**:

```
각 COMBO_RANGE에 대해:
  1. 해당 question_number 범위 질문 필터링
  2. 질문 수 = count 확인
  3. 모든 질문이 isValidForCombo() 통과
  4. topic 추출 → 유니크 → 쉼표 연결
  5. question_ids 추출
  6. ExtractedCombo 생성

→ 최대 5개 반환 (유효하지 않은 콤보 스킵)
```

**안전 설계**:

```
- try-catch 전체 감싸기 → 추출 실패 시 빈 배열 반환
- 콤보 0개도 정상 (후기는 항상 저장)
- 콤보는 빈도 분석 "부가 데이터" — 필수 아님
```

---

## Zod 검증 스키마

> `lib/validations/reviews.ts`

### step1Schema

```typescript
const step1Schema = z.object({
  // 시험 정보
  exam_date: z.string().min(1),
  exam_difficulty: z.string().min(1),
  pre_exam_level: z.enum(PRE_EXAM_LEVELS),
  achieved_level: z.string(),  // 'unknown' 허용

  // 시험 배경
  exam_purpose: z.enum(EXAM_PURPOSES),
  study_methods: z.array(z.enum(STUDY_METHODS)).min(1),
  prep_duration: z.enum(PREP_DURATIONS),
  attempt_count: z.enum(ATTEMPT_COUNTS),

  // 체감 후기
  perceived_difficulty: z.enum(PERCEIVED_DIFFICULTIES),
  actual_duration: z.enum(ACTUAL_DURATIONS),

  // 서베이
  used_recommended_survey: z.boolean(),

  // 커스텀 서베이 (조건부)
  survey_occupation: z.string().optional(),
  survey_student: z.string().optional(),
  survey_course: z.string().optional(),
  survey_housing: z.string().optional(),
  survey_leisure: z.array(z.string()).optional(),
  survey_hobbies: z.array(z.string()).optional(),
  survey_sports: z.array(z.string()).optional(),
  survey_travel: z.array(z.string()).optional(),
})
.refine((data) => {
  if (data.used_recommended_survey) return true;
  // 커스텀: 라디오 4개 필수
  if (!data.survey_occupation || !data.survey_student ||
      !data.survey_course || !data.survey_housing) return false;
  // 체크박스 합산 12개 이상
  const checkboxTotal =
    (data.survey_leisure?.length ?? 0) +
    (data.survey_hobbies?.length ?? 0) +
    (data.survey_sports?.length ?? 0) +
    (data.survey_travel?.length ?? 0);
  return checkboxTotal >= 12;
});
```

### step2Schema

```typescript
const step2Schema = z.object({
  submission_id: z.number(),
  questions: z.array(z.object({
    question_number: z.number().min(2).max(15),
    combo_type: z.enum(COMBO_TYPES),
    topic: z.string().min(1),
    question_id: z.string().nullable(),
    custom_question_text: z.string().nullable(),
    is_not_remembered: z.boolean(),
  })).length(14),
});
```

### step3Schema

```typescript
const step3Schema = z.object({
  submission_id: z.number(),
  one_line_review: z.string().min(1).max(100),
  tips: z.string().max(300).optional(),
});
```

---

## 서버 사전로드 (페이지 서버 컴포넌트)

### /reviews 페이지

```typescript
// app/(dashboard)/reviews/page.tsx
export default async function ReviewsPage() {
  const [statsAndFrequency, mySubmissions, publicReviews] = await Promise.all([
    getStatsAndFrequency(),
    getMySubmissions(),
    getPublicReviews({ page: 1, limit: 10 }),
  ]);

  // 완료 후기 ID 추출 → 일괄 상세 조회
  const completedIds = mySubmissions
    .filter(s => s.status === "complete")
    .map(s => s.id);
  const submissionDetails = completedIds.length > 0
    ? await getSubmissionsWithQuestionsBatch(completedIds)
    : {};

  return (
    <ReviewsContent
      initialStats={statsAndFrequency.stats}
      initialFrequency={statsAndFrequency.frequency}
      initialSubmissions={mySubmissions}
      initialPublicReviews={publicReviews}
      initialSubmissionDetails={submissionDetails}
    />
  );
}
```

### /reviews/submit 페이지

```typescript
// app/(immersive)/reviews/submit/page.tsx
export default async function SubmitPage({ searchParams }) {
  const resumeId = searchParams.resume;

  return (
    <ReviewWizard
      resumeSubmissionId={resumeId ? Number(resumeId) : undefined}
    />
  );
}
```

---

## 에러 처리 패턴

| 상황 | 처리 |
|------|------|
| 인증 실패 | `throw new Error("로그인이 필요합니다")` |
| draft 없음 (이어쓰기) | `throw new Error("작성 중인 후기를 찾을 수 없습니다")` |
| complete 삭제 시도 | `throw new Error("완료된 후기는 삭제할 수 없습니다")` |
| 중복 complete | UPDATE 0건 (이미 complete) → 에러 없이 반환 |
| 콤보 추출 실패 | try-catch → console.error → 후기는 유지 |
| 크레딧 RPC 실패 | try-catch → console.error → credit_granted=false |
| Supabase 쿼리 실패 | `if (error) throw error` → 클라이언트에 에러 전파 |
