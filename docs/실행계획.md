# 오픽톡닥 (OPIcTalkDoc) 실행 계획서

> **이 문서 하나만 보면 오픽톡닥 개발 전체를 진행할 수 있다.**
> 각 단계에서 참조할 설계 문서를 명시해두었으니, 필요할 때 해당 문서를 펼쳐보면 된다.
> 의사결정 근거는 `의사결정.md` 참조.

---

## 관련 문서

| 문서 | 역할 | 언제 보는가 |
|------|------|------------|
| **이 문서 (실행계획.md)** | 마스터 실행 가이드 | 항상 — 다음에 뭘 할지 여기서 확인 |
| `의사결정.md` | 제품/기술 의사결정 기록 | 결정 근거 확인, 미결 사항 추적 |
| `설계/공통기반.md` | DB 원칙, 백엔드 아키텍처 | 모듈 구현 전 공통 원칙 확인 |
| `설계/시험후기.md` ~ `설계/쉐도잉.md` | 모듈별 상세 설계 | 해당 모듈 구현 시 |
| `참조/소리담_기능분석.md` | 이관 요약 + 분석결과 인덱스 | 소리담 원본 구조 확인 시 |
| `참조/소리담_분석결과.md` | 상세 레퍼런스 (60KB) | 소리담 스키마, API, 타입 상세 |
| `사업운영.md` | 사업/행정/결제 | 결제 연동, PG사 계약 확인 시 |
| `디자인시스템.md` | 아토믹 디자인 시스템 | UI 컴포넌트 구현 시 |

---

## 개발 원칙

- **소리담 최종 버전만 이관**: 모든 레거시(v4/v6) 무시, 최종 버전(v7 등)만 가져옴
- **오픽톡닥에서 모두 v1**: 소리담 버전 접미사 제거 (예: `evaluations_v7` → `evaluations`)
- **인증/결제는 새로 구축**: 소리담 코드 복사 X → 업계 표준 방식
- **DB는 새로 설계**: 소리담 스키마를 참고하되 정리해서 새로 만듦
- **하이브리드 백엔드** (T-9): CRUD = Server Actions, AI API 호출 = Edge Functions, 웹훅 = API Routes
- **Server Actions**: 데이터 조회(Server Component), 데이터 변경(Server Action) — 시험후기 전체, 나머지 모듈의 CRUD
- **Edge Functions**: GPT/Whisper/Gemini TTS/Azure Speech 등 장시간 AI 처리 — 스크립트/모의고사/튜터링의 AI 호출
- **UI는 오픽톡닥 브랜드**: Tailwind 4.x + 테라코타 웜톤 디자인 시스템
- **모듈별 풀스택 이관**: DB → RLS → Server Actions → TypeScript 타입 → UI → 테스트

---

## 개발 순서 총괄

```
Phase 1 (인증) ✅ 완료
    ↓
Phase 2 (랜딩/필수 페이지) ✅ 완료
    ↓
Phase 3 (핵심 모듈 이관) ← 현재 단계 (Step 3 완료 → EF 배포 + 통합 테스트 → Step 4 튜터링 → 리브랜딩)
    ↓
Phase 4 (결제 연동) ✅ 기본 완료 (PG사 본계약 진행 중)
    ↓
Phase 5 (관리자)
```

---

## Phase 1. 인증 시스템 ✅ 완료

> Supabase Auth 기반, 업계 표준 방식으로 새로 구축.

| 항목 | 상태 |
|------|------|
| Supabase Auth 설정 (이메일 + Google OAuth) | ✅ |
| 로그인/회원가입 페이지 | ✅ |
| 비밀번호 찾기/재설정 | ✅ |
| 프로필 설정/수정 (마이페이지) | ✅ |
| 미들웨어 세션 관리 | ✅ |
| Server Actions (auth.ts) | ✅ |

---

## Phase 2. 랜딩 페이지 & 필수 페이지 ✅ 완료

> 카드사 심사 요건 충족 + 서비스 소개.

| 항목 | 상태 |
|------|------|
| 랜딩 페이지 ("나는 내 삶의 주인공이다" 무대 메타포) | ✅ |
| 요금제 페이지 (무료/베이직 ₩19,900/프리미엄 ₩49,900 + 횟수권) | ✅ |
| 이용약관/개인정보처리방침/환불규정 | ✅ |
| Navbar + Footer 공통 레이아웃 | ✅ |
| 사업자정보 푸터 (카드사 심사 요건) | ✅ |
| 전략 가이드 페이지 (/strategy, 10섹션) | ✅ |
| 넛지 배너 + 등급 설정 모달 (P-1) | ✅ |
| 대시보드 실데이터 연동 (P-3, 1단계) | ✅ |
| 로고 이미지 시스템 (Canvas API + Jua 폰트, 8종) | ✅ |

---

## Phase 3. 핵심 모듈 이관 ← 현재 단계

> 소리담에서 선별한 기능을 오픽톡닥에 이관.
> **이 Phase가 개발의 핵심이며, 모듈별로 순서대로 진행한다.**
> 백엔드 아키텍처: 하이브리드 — CRUD=Server Actions, AI=Edge Functions (T-9)

### 레이아웃 구조 (확정)

| 레이아웃 | 용도 | 포함 |
|----------|------|------|
| **(dashboard)** | 탐색/허브 페이지 | Navbar + Footer |
| **(immersive)** | 활동/몰입 페이지 | ImmersiveHeader만 |

### 네비게이션 + 모듈별 탭 구조 (확정)

```
대시보드 | 시험후기 | 스크립트 | 모의고사 | 튜터링
```

| 모듈 | 경로 | 탭 | UI 셸 |
|------|------|-----|--------|
| **시험후기** | `/reviews` | 빈도 분석 \| 후기 제출 \| 시험 후기 | ✅ 완료 |
| **스크립트** | `/scripts` | 스크립트 생성 \| 내 스크립트 \| 쉐도잉 훈련 | ✅ 완료 |
| **모의고사** | `/mock-exam` | 응시 \| 결과 \| 나의 이력 | ✅ 완료 |
| **튜터링** | `/tutoring` | 진단 \| 처방 \| 훈련 | ✅ 완료 |

> 쉐도잉은 스크립트와 동일 연장선(패키지 생성 → 쉐도잉 훈련)이므로 스크립트 모듈에 통합

### 몰입형 페이지 (확정)

| 페이지 | 경로 | 설명 |
|--------|------|------|
| 모의고사 세션 | `/mock-exam/session` | 15문제 순차 진행 |
| 스크립트 생성 | `/scripts/create` | AI 스크립트 생성 |
| 쉐도잉 훈련 | `/scripts/shadowing` | 4단계 점진 훈련 (듣기/따라읽기/혼자말하기/실전) |
| 튜터링 훈련 | `/tutoring/training` | 4레벨 훈련 |

### 이관 순서 (확정)

```
Step 0: 핵심 코어 (questions) ✅ 완료
    ↓
Step 1: 시험후기 ✅ 완료 (Server Actions only)
    ↓
Step 2: 스크립트+쉐도잉 ✅ 완료 (SA + EF + 패키지 EF + 쉐도잉 4단계 UI)
    ↓
Step 3: 모의고사 ✅ 완료 (SA 10개 + EF 3개 + 공유모듈 4개 + UI 11개 + 훅 3개)
    ↓
Step 4: 튜터링 ⏳ (Server Actions + Edge Functions)
```

### 각 모듈 이관 절차 (공통)

```
1. DB 설계         → Supabase 마이그레이션 SQL 작성
2. RLS 정책        → Row Level Security 설정
3. Server Actions  → 'use server' CRUD 함수 작성
4. Edge Functions  → AI API 호출 함수 작성 (T-9, 해당 모듈만)
5. TypeScript 타입 → 프론트에서 쓸 타입 정의
6. 상태관리        → Zustand 스토어 (필요 시)
7. UI 컴포넌트     → 페이지 + 컴포넌트 구현
8. 테스트          → 동작 확인
```

---

### Step 0: 핵심 코어 — questions (SSOT) ✅ 완료

> **모든 모듈의 기반. 이것 없이는 아무것도 동작하지 않는다.**
> 📖 DB 원본: `docs/질문 DB/questions_db.xlsx`

#### 완료 내역

| 항목 | 상태 |
|------|------|
| (구) `master_questions` 테이블 생성 (510행, 소리담 이관) | ✅ → `questions`로 전환 완료 |
| `questions` 테이블 생성 (471행, 13컬럼, 009_questions.sql) | ✅ |
| Storage 버킷 `audio-recordings` 생성 | ✅ |
| 기존 모듈 FK 참조 `master_questions` → `questions` 전환 | ✅ |

#### M-1 결정 사항

`custom_mode_questions` 테이블은 **삭제** (의사결정 M-1).
→ `submission_combos` 테이블로 대체. Step 1 시험후기에서 함께 설계.

#### questions 테이블 구조 — 471행, 13컬럼 (D-1 확정, 009_questions.sql)

| 컬럼 | 설명 | 예시 |
|------|------|------|
| `id` | 토픽코드_카테고리_서베이_타입_번호 | `FRN_GEN_COM_DESC_01` |
| `category` | 일반 / 롤플레이 / 어드밴스 / 시스템 | `일반` |
| `sub_category` | Q11~Q15 또는 `-` | `Q12` |
| `topic` | 주제명 | `가구` |
| `survey_type` | 공통형 / 선택형 / 시스템 | `공통형` |
| `question_english` | 영문 질문 원문 | `Tell me about...` |
| `question_korean` | 한글 번역 | `현재 집에 있는...` |
| `question_short` | 질문 요약 (한줄) | `집 가구와 가장 좋아하는 가구` |
| `question_type_kor` | 한글 질문 유형 | `묘사` |
| `question_type_eng` | 영문 질문 유형 | `description` |
| `tag` | OPIc 등급 태그 | `Int` / `Adv` |

#### 카테고리별 분포

| 카테고리 | 질문 수 | 토픽 수 |
|----------|---------|---------|
| 일반 | 223개 | 36개 |
| 롤플레이 | 183개 | 38개 (Q11:59 + Q12:62 + Q13:62) |
| 어드밴스 | 63개 | 20개 (Q14:31 + Q15:32) |
| 시스템 | 2개 | 2개 |

#### question_type 10종 (반드시 기억)

| question_type_kor | question_type_eng | 수량 | tag | 카테고리 |
|-------------------|-------------------|------|-----|----------|
| 묘사 | description | 60 | Int | 일반 |
| 루틴 | routine | 47 | Int | 일반 |
| 비교 | comparison | 16 | Int | 일반 |
| 경험_어릴적/처음 | past_childhood | 21 | Adv | 일반 |
| 경험_최근 | past_recent | 18 | Adv | 일반 |
| 경험_특별한 | past_special | 115 | Adv | 일반+롤플레이 |
| 비교/변화 | adv_14 | 31 | Adv | 어드밴스 Q14 |
| 사회적이슈 | adv_15 | 32 | Adv | 어드밴스 Q15 |
| 질문하기 | rp_11 | 59 | Adv | 롤플레이 Q11 |
| 대안제시 | rp_12 | 62 | Adv | 롤플레이 Q12/Q13 |

---

### Step 1: 시험후기 ✅ 완료

> 시험 후기 제출 (3단계 위저드) + 출제 빈도 분석 + 공개 후기 열람 + 콤보 데이터 생성.
> 📖 상세 설계: `설계/시험후기.md`
> 📖 소리담 원본: `참조/소리담_분석결과.md` §6

#### 완료 내역 (2026-02-23)

| 항목 | 상태 |
|------|------|
| DB 3테이블 (submissions, submission_questions→questions FK, submission_combos) | ✅ |
| RLS 정책 (본인 CRUD + complete 전체 읽기) | ✅ |
| 인덱스 6개 + updated_at 트리거 | ✅ |
| increment_script_credits RPC 함수 | ✅ |
| TypeScript 타입 + Zod 스키마 (types/reviews.ts, validations/reviews.ts) | ✅ |
| 콤보 추출기 (utils/combo-extractor.ts) | ✅ |
| Server Actions 12개 (actions/reviews.ts) | ✅ |
| questions 쿼리 (queries/master-questions.ts) | ✅ |
| 위저드 3단계 UI (wizard-step1/2/3.tsx) | ✅ |
| 주제 선택 + 질문 선택 컴포넌트 (topic-pagination, question-selector) | ✅ |
| 빈도 분석 탭 (frequency-tab.tsx) | ✅ |
| 후기 제출 탭 (submit-tab.tsx) | ✅ |
| 공개 후기 탭 (list-tab.tsx) | ✅ |
| 크레딧 보상 P-2 (월 2건 제한, script_credits +2) | ✅ |

#### 파일 목록 (15개 생성 + 2개 수정)

```
lib/
│   ├── types/reviews.ts          (생성)
│   ├── validations/reviews.ts    (생성)
│   ├── utils/combo-extractor.ts  (생성)
│   ├── actions/reviews.ts        (생성)
│   └── queries/master-questions.ts (생성)
├── components/reviews/
│   ├── reviews-content.tsx       (수정)
│   ├── frequency/frequency-tab.tsx (생성)
│   ├── submit/
│   │   ├── submit-tab.tsx        (생성)
│   │   ├── wizard-step1.tsx      (생성)
│   │   ├── wizard-step2.tsx      (생성)
│   │   ├── wizard-step3.tsx      (생성)
│   │   ├── topic-pagination.tsx  (생성)
│   │   └── question-selector.tsx (생성)
│   └── list/list-tab.tsx         (생성)
└── app/(dashboard)/reviews/page.tsx (수정)
supabase/migrations/003_submissions.sql (생성)
```

#### 미결 사항
- **M-3**: 소리담 기존 후기 데이터 이관 여부 ⏳

---

### Step 2: 스크립트+쉐도잉 ✅ 완료

> AI 스크립트 생성 + 패키지(WAV+JSON) + 내 스크립트 관리 + 쉐도잉 5단계 훈련 + AI 평가.
> 📖 상세 설계: `설계/스크립트.md`, `설계/쉐도잉.md`
> 📖 소리담 원본: `참조/소리담_분석결과.md` §9, §10, §11

#### 완료 내역

| 항목 | 상태 |
|------|------|
| DB 6테이블 + opic_tips 테이블 (004, 005 마이그레이션) | ✅ |
| RLS 정책 + 인덱스 8개 + RPC 함수 2개 + Storage 버킷 2개 | ✅ |
| ai_prompt_templates 시드 3행 + script_specs 시드 60행 | ✅ |
| TypeScript 타입 + Zod 스키마 7개 | ✅ |
| Server Actions 16개 | ✅ |
| EF scripts/ — generate/correct/refine/evaluate 4라우트 (Two-Pass + 평가) | ✅ |
| EF scripts-package/ — generate-package/generate-shadowing 2라우트 (TTS + 타임스탬프) | ✅ |
| EF Supabase 배포 완료 (scripts + scripts-package 2개 함수) | ✅ |
| 5단계 위저드 (질문선택 → 옵션설정 → opic_tips 대기화면 → 결과확인 → 확정+패키지생성) | ✅ |
| 스크립트 뷰어 2탭(스크립트/핵심정리) + 4모드(영/한같이, 영어만, 한글만, 영/한구분) | ✅ |
| 핵심 정리 인터랙티브 하이라이트 (카테고리 뱃지 클릭 → 본문 강조) | ✅ |
| 4계층 단락 렌더링 (Introduction/Body/Conclusion 헤더 + 슬롯별 정렬) | ✅ |
| 재생성 기능 (스토리 수정 + 확인 모달 + 크레딧 차감 + generate 재호출) | ✅ |
| scripts-content 3탭 TanStack Query + 서버 병렬 조회 | ✅ |
| TopicPagination 컴포넌트 재활용 (시험후기 + 스크립트 공유) | ✅ |
| 목표 등급 미설정 시 GradeSettingModal 인라인 게이트 | ✅ |
| Step5Complete 패키지 생성 UI (음성 선택 + 프로그레스 + 쉐도잉 시작 링크) | ✅ |
| 내 스크립트 탭 패키지 상태 표시 (생성/처리중/완료/부분완료 뱃지) | ✅ |
| Zustand 쉐도잉 스토어 (4단계 상태 + localStorage persist) | ✅ |
| 쉐도잉 UI 10개 컴포넌트 (player/recorder/step-nav + Step 1~4 + 평가 결과/이력) | ✅ |
| 쉐도잉 페이지 재작성 (서버 사전 조회 + 키보드 단축키 1~4) | ✅ |
| evaluate_shadowing 프롬프트 상세화 (ACTFL 기준 5영역 가이드라인) | ✅ |

#### 파일 목록

```
supabase/
├── migrations/004_scripts.sql                (생성)
├── migrations/005_opic_tips.sql              (생성)
├── functions/scripts/index.ts                (생성 → 수정: evaluate 라우트 추가)
└── functions/scripts-package/index.ts        (생성: TTS + 타임스탬프 패키지)
lib/
│   ├── types/scripts.ts                      (생성)
│   ├── validations/scripts.ts                (생성)
│   ├── actions/scripts.ts                    (생성 → 수정: 5 SA 추가)
│   ├── stores/shadowing.ts                   (생성: Zustand 스토어)
│   └── queries/master-questions.ts           (수정: +getTopics, +getQuestionsByCategory — questions 테이블 조회)
├── components/scripts/
│   ├── scripts-content.tsx                   (수정: TanStack Query + 패키지 상태 + 쉐도잉 탭)
│   └── create/
│       ├── script-wizard.tsx                 (생성 → 수정: Step5Complete 패키지 생성)
│       └── script-renderer.tsx               (생성: 4모드 뷰어 + 핵심정리)
├── components/shadowing/                     (전체 신규 — 11개 컴포넌트)
│   ├── shadowing-content.tsx                 (메인 래퍼 + 키보드 단축키)
│   ├── shadowing-player.tsx                  (오디오 플레이어 + 문장 하이라이트)
│   ├── shadowing-recorder.tsx                (MediaRecorder 녹음)
│   ├── shadowing-step-nav.tsx                (4단계 네비게이션)
│   ├── step-listen.tsx                       (Step 1: 전체 듣기 + 표시 모드)
│   ├── step-shadow.tsx                       (Step 2: 따라읽기 + 텍스트 힌트 토글)
│   ├── step-recite.tsx                       (Step 3: 혼자 말하기 + 타이머 + peek)
│   ├── step-speak.tsx                        (Step 4: 실전 녹음 + AI 평가)
│   ├── evaluation-result.tsx                 (5영역 점수 + OPIc 등급 + 피드백)
│   └── evaluation-history.tsx                (평가 이력 목록)
└── app/
    ├── (dashboard)/scripts/page.tsx          (수정: 서버 병렬 조회)
    ├── (immersive)/scripts/create/page.tsx    (수정: ScriptWizard 연결)
    └── (immersive)/scripts/shadowing/page.tsx (재작성: 서버 사전 조회 + ShadowingContent)
```

#### DB 테이블 (6개)

| 테이블 | 용도 | 소리담 대비 변경 |
|--------|------|------------------|
| `scripts` | 스크립트 마스터 (생성+교정 공유, source 구분) | ai_training_scripts → scripts 통합 |
| `script_packages` | TTS 음성 + 타임스탬프 (WAV+JSON) | PDF 제거 |
| `ai_prompt_templates` | RCTF 프롬프트 (스크립트+튜터링 공유, 2행) | 3가지 → System/User 분리 |
| `script_specs` | 등급별 규격서 (60행, 4컬럼 분할) | 신규 |
| `shadowing_sessions` | 쉐도잉 실전 세션 (Step 5만) | 만료 없음, mode 컬럼 제거 |
| `shadowing_evaluations` | AI 평가 (5영역 + OPIc 등급) | shadowing_attempts → evaluations |

#### Server Actions (16개)

| Action | 용도 |
|--------|------|
| `checkScriptCredit` | 크레딧 잔량 확인 |
| `createScript` | 스크립트 생성 (크레딧 차감 + UPSERT) |
| `createCorrectScript` | 답변 교정 (튜터링 모듈 Step 4에서 사용) |
| `refineScript` | 수정 요청 (최대 3회, 크레딧 무소모) |
| `confirmScript` | 스크립트 확정 (draft → confirmed) |
| `deleteScript` | 삭제 (패키지 Storage 파일 정리 포함) |
| `getMyScripts` | 내 스크립트 목록 (패키지 JOIN) |
| `getScriptDetail` | 상세 (questions JOIN) |
| `getScriptSpec` | 등급별 규격서 조회 |
| `getShadowingHistory` / `getScriptStats` | 쉐도잉 이력 + 통계 |
| `createPackage` | 패키지 생성 (2단계 EF 호출: TTS → 타임스탬프) |
| `getShadowingData` | 쉐도잉 데이터 로드 (wavUrl + sentences + keyExpressions) |
| `startShadowingSession` | Step 5 세션 생성 (shadowing_sessions INSERT) |
| `getShadowingEvaluation` | 평가 상세 조회 (shadowing_evaluations) |
| `getShadowableScripts` | 쉐도잉 가능 스크립트 목록 (패키지 완료 기준) |

#### Edge Functions (2개 함수)

**scripts/ (4 라우트)**

| Route | 용도 |
|-------|------|
| `generate` | GPT-4.1 스크립트 생성 (RCTF 프롬프트 + script_specs 주입) |
| `correct` | GPT-4.1 답변 교정 (튜터링 모듈 Step 4에서 사용) |
| `refine` | GPT-4.1 수정 (기존 스크립트 + 유저 요청 기반) |
| `evaluate` | Whisper STT + GPT-4.1 5영역 평가 (크레딧 차감/환불) |

**scripts-package/ (2 라우트)**

| Route | 용도 |
|-------|------|
| `generate-package` | ElevenLabs TTS → MP3 → Storage 업로드 (progress 60%) |
| `generate-shadowing` | Whisper STT → 문장-단어 매칭 → JSON → Storage (progress 100%) |

#### 핵심 변경

- scripts 테이블 `UNIQUE(user_id, question_id)` → 스크립트+튜터링이 동일 테이블 공유
- **스크립트 UI는 generate 전용** — correct 모드(답변 교정)는 튜터링 모듈(Step 4)에서 사용. 백엔드 코드(SA+EF) 보존
- `source='generate'` (스크립트 모듈), `source='correct'` (튜터링 교정)
- **RCTF 프롬프트**: System Prompt(고정, 캐시) + User Prompt(동적, script_specs 주입)
- **4계층 JSON**: paragraphs > slots > sentences > parts (T/E/F 분류)
- **쉐도잉 4단계 점진 훈련**: 듣기/따라읽기(클라이언트) + 혼자말하기(클라이언트) + 실전(API)
- 소리담 4모드가 아닌 학술 BP 기반 신규 설계 (Kadota 2019 + ELT School 5단계)
- **패키지 2단계 생성**: Phase 1 TTS(60%) → Phase 2 타임스탬프(100%), 부분 실패 시 `partial` 상태
- **쉐도잉 Step 2 따라읽기**: 텍스트 힌트 토글 (전체/첫단어/숨김) — 사용자가 자유롭게 전환
- **쉐도잉 Step 4 실전 평가**: 크레딧 1개 차감, 5단어 미만 자동 환불, API 실패 시 환불

---

### Step 3: 모의고사 ✅ 완료

> 15문제 세션 + V7 평가 (체크박스 + 규칙엔진) + FACT 점수 리포트.
> 📖 상세 설계: `설계/모의고사.md`
> 📖 소리담 분석: `설계/모의고사_소리담분석.md`, `설계/모의고사_코드플로우.md`

#### 완료 내역 (2026-03-02 ~ 03-03)

| 항목 | 상태 |
|------|------|
| DB 5테이블 (sessions, answers, evaluations, reports, evaluation_queue) + RLS + 인덱스 | ✅ |
| RPC 함수 2개 (consume_mock_exam_credit, refund_mock_exam_credit) | ✅ |
| Storage 버킷 `mock-test-recordings` (공개 읽기, 인증 업로드) | ✅ |
| TypeScript 타입 + Zod 스키마 (types/mock-exam.ts, validations/mock-exam.ts) | ✅ |
| Server Actions 10개 (actions/mock-exam.ts) | ✅ |
| EF 공유 모듈 4개 (_shared/: azure-pronunciation, skip-detector, checkbox-definitions, rule-engine) | ✅ |
| EF Stage A: mock-test-process (Whisper STT + 스킵 판정 + Azure 발음) | ✅ |
| EF Stage B: mock-test-eval (GPT-4.1 체크박스 평가, question_type→prompt 매핑) | ✅ |
| EF Stage C: mock-test-report (V7 규칙엔진 7-Step + FACT 100점 + GPT 종합 리포트) | ✅ |
| 커스텀 훅 3개 (use-recorder, use-question-player, use-eval-polling) | ✅ |
| UI: 모드 선택 + 기출 풀 선택 + 디바이스 테스트 + 세션 래퍼(복원 UX) | ✅ |
| UI: 세션 진행 (AVA 아바타, 녹음+볼륨+경고, 5단계 가이드, 15문항 그리드, 타이머) | ✅ |
| UI: 평가 대기 (폴링 진행률) + 결과 요약 (7섹션) + 결과 상세 (문항별 아코디언) | ✅ |
| 세션 플로우: 자동재생 제거 (질문 듣기 클릭 시작), 5초 다시듣기, 10초 후 다음 활성화 | ✅ |
| use-recorder: reset() onstop 레이스 컨디션 수정 (cancelledRef) | ✅ |
| 네비게이션 버그: router.push 후 setState 제거 (React concurrent mode 충돌 해결) | ✅ |
| Q1 자기소개: questions 테이블 audio_url 사용 (Q2~Q15과 동일 플로우) | ✅ |

#### DB 테이블 (5개)

| 테이블 | 용도 | 소리담 대비 변경 |
|--------|------|------------------|
| `mock_test_sessions` | 세션 (question_ids TEXT[], mode, status, 72h/90min) | JSONB → TEXT[] |
| `mock_test_answers` | 답변 (audio_url, eval_status 7단계) | queue 통합 |
| `mock_test_evaluations` | 개별 평가 (STT + 발음 + GPT 체크박스) | v7 접미사 제거 |
| `mock_test_reports` | 종합 리포트 (FACT, 등급, GPT 총평) | holistic → reports |
| `mock_test_evaluation_queue` | 평가 큐 (EF fire-and-forget 체인용) | 신규 |

#### Server Actions (10개)

| Action | 용도 |
|--------|------|
| `getExamPools` | 기출 풀 목록 (승인 submissions 조회) |
| `createSession` | 세션 생성 + 크레딧 차감 + 15문항 배정 (Q1 포함) |
| `getSession` | 세션 상세 (answers + evaluations + report + questions JOIN) |
| `submitAnswer` | 답변 저장 (Q1 eval_status=skipped, Q2~15=pending) |
| `completeSession` | 세션 완료 + 평가 큐 트리거 |
| `getHistory` | 이력 목록 (모드 필터 + FACT 점수) |
| `getSessionDetail` | 결과 상세 (evaluations + report JOIN) |
| `abandonSession` | 세션 포기 + 크레딧 환불 |
| `getActiveSession` | 활성 세션 확인 (이어하기 표시용) |
| `expireSession` | 만료 처리 |

#### Edge Functions (3개 함수 + 4개 공유 모듈)

**공유 모듈 (`_shared/`)**

| 파일 | 용도 |
|------|------|
| `skip-detector.ts` | 3단계 스킵 판정 (15초 미만/15자 미만/환청 패턴) |
| `azure-pronunciation.ts` | Azure Speech SDK WebSocket 발음 평가 |
| `checkbox-definitions.ts` | 74개 체크박스 ID 정의 + FACT 4영역 매핑 + 누적 로직 |
| `rule-engine.ts` | V7 규칙엔진 7-Step (Floor→Ceiling→SL→Q12→AL→최종등급) + FACT 100점 계산 |

**EF 3개**

| EF | 역할 | 트리거 |
|----|------|--------|
| `mock-test-process/` | Stage A: Whisper STT + 스킵 판정 + Azure 발음 | SA fire-and-forget |
| `mock-test-eval/` | Stage B: GPT-4.1 체크박스 평가 (question_type별 프롬프트) | Stage A fire-and-forget |
| `mock-test-report/` | Stage C: 규칙엔진 + FACT + GPT 종합 리포트 | Stage B fire-and-forget |

#### 커스텀 훅 (3개)

| 훅 | 용도 |
|----|------|
| `use-recorder` | MediaRecorder 녹음 (볼륨 분석, 무음 감지, 시간 경고, cancelledRef) |
| `use-question-player` | 질문 오디오 재생 + 5초 리플레이 윈도우 + 자동 녹음 콜백 |
| `use-eval-polling` | 평가 상태 폴링 (5초 간격, Supabase 직접 조회) |

#### UI 컴포넌트 (11개 신규)

| 컴포넌트 | 용도 |
|----------|------|
| `mode-selector.tsx` | 훈련/실전 모드 선택 |
| `exam-pool-selector.tsx` | 기출 풀 카드 선택 |
| `device-test.tsx` | 마이크 권한 + 볼륨 테스트 |
| `mock-exam-session-wrapper.tsx` | 세션 래퍼 (로딩/복원/디바이스테스트/세션/에러) |
| `mock-exam-session.tsx` | 시험 진행 메인 (AVA + 녹음 + 5단계 가이드 + 업로드) |
| `session-timer.tsx` | 타이머 (훈련: 경과시간, 실전: 40분 카운트다운) |
| `question-grid.tsx` | 15문항 상태 그리드 |
| `ava-avatar.tsx` | AI 면접관 아바타 (speaking/listening 상태) |
| `eval-waiting.tsx` | 평가 대기 + 폴링 진행률 |
| `result-summary.tsx` | 결과 요약 (등급+FACT+영역+발음+권장+피드백) |
| `result-detail.tsx` | 문항별 상세 아코디언 (체크박스/교정/발음) |

#### 핵심 변경 (소리담 대비)

- **CRUD**: Server Actions (T-9). CRUD EF 없음
- **오디오 업로드**: Client → Supabase Storage 직통 (Vercel 4.5MB 제한 회피)
- **출제**: 승인된 submission 통째로 (기출 세트, F-1)
- **3단계 AI 평가**: Stage A (STT+발음) → Stage B (GPT 체크박스) → Stage C (규칙엔진+리포트)
- **상태 모니터링**: 폴링 5초 + Supabase 직접 조회 (Vercel 무관, $0)
- **규칙엔진**: TypeScript (LLM 아님), Floor/Ceiling/AL/Gate-Keeper 판정
- **Q1 자기소개**: questions 테이블에서 조회 (SLF_SYS_SYS_UNK_01, audio_url 포함)
- **세션 플로우**: 자동재생 X → "질문 듣기" 클릭 → 오디오 재생 → 자동 녹음 → 5초 다시듣기 → 10초 후 다음
- **녹음 시스템**: MediaRecorder WebM + cancelledRef (reset 레이스 컨디션 방지)

---

### Step 4: 튜터링 (구 AI 튜터)

> 모의고사 결과 기반 진단 → 처방 → 4레벨 훈련.
> 📖 상세 설계: `설계/튜터링.md`
> 📖 소리담 원본: `참조/소리담_분석결과.md` §8

#### DB 테이블 (6개 + scripts 공유)

| 테이블 | 용도 | 소리담 대비 변경 |
|--------|------|------------------|
| `tutoring_sessions` | 세션 (만료 없음) | expires_at 제거 |
| `tutoring_progress` | 유형별 진행 (level1~4 상태) | current_level → 각 레벨별 상태 |
| `tutoring_attempts` | 훈련 시도 (score, slot_evaluations) | 이름 변경 |
| `tutoring_slot_templates` | 슬롯 뼈대 (시드 데이터) | 유지 |
| `tutoring_review_schedule` | 복습 스케줄 (1→3→7일) | **신규** |
| `tutoring_skill_history` | 성장 추적 (pass_rate 추이) | **신규** |

> 교정 스크립트는 `scripts` 테이블 공유 (`source='correct'`)

#### Server Actions

| Action | 용도 |
|--------|------|
| `startSession` | 세션 시작 + 약점 분석 |
| `getDiagnosis` / `getProgress` | 진단/진행 조회 |
| `generateCorrectedScript` | AI 교정 (scripts에 UPSERT) |
| `evaluateAttempt` | Level 1~4 훈련 평가 |
| `nextQuestion` | 다음 유형 활성화 |
| `getReviewSchedule` / `getSkillHistory` | 복습/성장 조회 |

#### 4레벨 재설계 (소리담 대비 변경)

| 레벨 | 소리담 | 오픽톡닥 |
|------|--------|----------|
| Level 1 | 드래그 앤 드롭 | **듣고 따라 읽기** (Read Aloud) |
| Level 2 | 키워드 보고 말하기 | **쉐도잉** (텍스트 없이 따라 말하기) |
| Level 3 | 기능만 보기 | **힌트 보고 말하기** (Guided Speaking) |
| Level 4 | 실전 | **Real Test** (동일) |

---

### 공통 인프라 (모든 모듈에서 사용)

> 📖 소리담 원본: `참조/소리담_분석결과.md` §12

#### 권한 체크 (소리담 대비 변경)

| 소리담 | 오픽톡닥 |
|--------|----------|
| AIPermissionService (포인트 시스템) | **구독 횟수 체크** (user_credits) |
| 포인트 50P/100P 차감 | 크레딧 1회 차감 (플랜 → 횟수권 순서) |

#### 외부 서비스 API 키 필요

| 서비스 | 용도 | 준비 상태 |
|--------|------|----------|
| OpenAI | GPT-4.1 (평가/생성), Whisper (STT) | ✅ .env.local 저장 완료 |
| ElevenLabs | TTS 음성 합성 (T-10, Gemini → 변경) | ✅ .env.local 저장 완료 |
| Azure | Speech Service (발음 평가) | ⏳ 소리담 Secrets 재사용 예정 |

---

## Phase 4. 결제 연동 ✅ 기본 완료

> 포트원 V2 기반 결제 시스템. 기본 구현 완료, PG사 본계약 진행 중.
> 📖 상세: `사업운영.md` §4

#### 완료 내역

| 항목 | 상태 |
|------|------|
| `@portone/browser-sdk` 설치 (포트원 V2) | ✅ |
| `orders` + `user_credits` DB 테이블 | ✅ |
| `/api/payment/verify` 결제 검증 API | ✅ |
| `/api/payment/webhook` 결제 취소 웹훅 | ✅ |
| 스토어 페이지 (4개 상품) | ✅ |
| KG이니시스 결제창 동작 확인 (프로덕션) | ✅ |

#### 상품 구성

| 상품 | 가격 | 내용 |
|------|------|------|
| 베이직 플랜 | ₩19,900 | 모의고사 3회 + 스크립트 10회 |
| 프리미엄 플랜 | ₩49,900 | 모의고사 10회 + 스크립트 30회 |
| 모의고사 횟수권 | ₩7,900 | 모의고사 3회 (영구) |
| 스크립트 횟수권 | ₩3,900 | 스크립트 5회 (영구) |

#### 남은 작업

| 항목 | 상태 | 비고 |
|------|------|------|
| KG이니시스 본계약 + 전자서명 | ⏳ | 가계약 기간 내 (~ 2026-03-19) |
| KG이니시스 정기결제 MID | ⏳ | 발급 대기 |
| 카카오페이 심사 완료 | ⏳ | 보완 회신 완료, 결과 대기 |
| 네이버페이 심사 완료 | ⏳ | 재심사 요청 완료, 결과 대기 |
| 토스페이 MID 발급 | ⏳ | 입점 정보 회신 완료, 대기 |
| 통신판매업 신고 | ⏳ | 에스크로 확인증 필요 (본계약 후) |

---

## Phase 5. 관리자 페이지

> Phase 3 완성 후 필요한 관리 기능을 결정.

| 후보 기능 | 우선순위 |
|----------|---------|
| questions 관리 (CRUD) | 높음 |
| 사용자 관리 | 높음 |
| 구독/결제 관리 | 높음 |
| 시드 데이터 관리 (스킬, 프롬프트, 규격서) | 중간 |
| 통계 대시보드 | 낮음 |

---

## 현재 진행 상태

| Phase | 상태 | 비고 |
|-------|------|------|
| Phase 1 (인증) | ✅ 완료 | Supabase Auth + 프로필 |
| Phase 2 (랜딩/필수) | ✅ 완료 | 브랜딩 리뉴얼 + 넛지 배너 + 대시보드 실데이터 |
| **Phase 3 (핵심 모듈)** | **🔄 진행 중** | **Step 3 ✅ → EF 배포 + 통합 테스트 → Step 4 튜터링 → 리브랜딩(P-5)** |
| Phase 4 (결제) | ✅ 기본 완료 | 포트원 V2 + KG이니시스, PG사 본계약 진행 중 |
| Phase 5 (관리자) | ⏳ 대기 | Phase 3 완료 후 |

### Phase 3 세부 진행

| Step | 모듈 | 상태 | 백엔드 | 비고 |
|------|------|------|--------|------|
| Step 0 | 핵심 코어 | ✅ 완료 | - | questions 471행 (master_questions에서 전환, 009_questions.sql), custom_mode_questions 삭제(M-1) |
| Step 1 | 시험후기 | ✅ 완료 | Server Actions only | DB 3테이블 + Actions 12개 + 위저드 UI + 빈도분석 + 공개후기 |
| Step 2 | 스크립트+쉐도잉 | ✅ 완료 | SA + EF | 6테이블+16SA+6EF(2함수)+위저드+뷰어+쉐도잉 4단계 UI+패키지 생성 EF+카테고리/주제 필터+CTA 개선 |
| - | D-1 DB 전면 교체 | ✅ 완료 | - | questions 테이블 471행 생성 완료 (009_questions.sql) |
| **Step 3** | **모의고사** | **✅ 완료** | **SA + EF** | **5테이블+10SA+3EF(4공유모듈)+V7 규칙엔진+훅 3개+UI 11개+세션 플로우 수정** |
| Step 4 | 튜터링 | ⏳ 대기 | SA + EF | 6테이블, 4레벨 재설계, GPT/Whisper |

### DB 현황 (현재 18개 테이블)

| 테이블 | 행 수 | 용도 |
|--------|-------|------|
| `questions` | 471행 | OPIc 문제 SSOT (13컬럼, 새 ID 체계, 009_questions.sql) |
| `profiles` | - | 사용자 프로필 |
| `orders` | - | 결제 주문 기록 |
| `user_credits` | - | 이용권/크레딧 (회원가입 트리거 자동 생성) |
| `submissions` | - | 시험후기 마스터 + 설문 (Step 1) |
| `submission_questions` | - | 기출 문제 14개 기록 (FK → questions, Step 1) |
| `submission_combos` | - | 콤보 데이터 (Step 1) |
| `ai_prompt_templates` | 3행 | RCTF System+User Prompt + evaluate_shadowing (Step 2) |
| `script_specs` | 60행 | 등급별 규격서 — 10 answer_types × 6 levels (Step 2) |
| `scripts` | - | 스크립트 마스터 — 생성+교정 통합 (Step 2) |
| `script_packages` | - | TTS 패키지 WAV+JSON (Step 2) |
| `shadowing_sessions` | - | 쉐도잉 세션 (Step 2) |
| `shadowing_evaluations` | - | 쉐도잉 AI 평가 (Step 2) |
| `mock_test_sessions` | - | 모의고사 세션 (mode, status, 15 question_ids, 72h/90min) (Step 3) |
| `mock_test_answers` | - | 답변 기록 (audio_url, eval_status 7단계) (Step 3) |
| `mock_test_evaluations` | - | 개별 평가 (STT + 발음 + GPT 체크박스) (Step 3) |
| `mock_test_reports` | - | 종합 리포트 (FACT 점수, 등급, GPT 총평) (Step 3) |
| `mock_test_evaluation_queue` | - | 평가 큐 (EF fire-and-forget 체인용) (Step 3) |

### 바로 다음 작업

#### ✅ Step 2 완료 사항 요약
- 패키지 생성 Edge Function (`scripts-package/`): generate-package + generate-shadowing 2라우트
- 쉐도잉 훈련 UI 11개 컴포넌트: player, step-nav, recorder, step 1~5, evaluation-result, evaluation-history
- Zustand store (`stores/shadowing.ts`): 5단계 상태 관리 + localStorage persist
- Server Actions 5개 추가: getShadowingData, createPackage, startShadowingSession, submitShadowing, getShadowableScripts
- evaluate 라우트 (`scripts/index.ts`): Whisper STT + GPT-4.1 ACTFL 평가
- Step5Complete 패키지 생성 대기 UX: OPIc 팁 자동 순환 + TipCard 공통 컴포넌트 + 시뮬레이션 프로그레스
- 쉐도잉 duplicate key 수정: GPT index description 추가 + extractSentences globalIndex 보정 + DB 기존 데이터 보정
- EF 2개 배포 완료: scripts, scripts-package (2026-02-26)
- 내 스크립트/쉐도잉 훈련 탭: 카테고리→주제 2단계 필터 + 페이지네이션 그리드 (2026-02-27)
- 스크립트 생성 탭: CTA 버튼 통합 ("스크립트 생성 시작하기"), "나만의 맞춤 스크립트" 문구 변경 (2026-02-27)

#### ✅ D-1: master_questions → questions 전환 완료 (2026-03-01)
- `009_questions.sql` 마이그레이션 실행 완료
- `questions` 테이블 471행 (일반 223 + 롤플레이 183 + 어드밴스 63 + 시스템 2)
- 13컬럼 (11 데이터 + created_at/updated_at), 인덱스 6개, RLS(전체 SELECT), updated_at 트리거
- `questions`가 새 SSOT — 기존 모듈 FK 참조 전환 완료

#### 🔄 리브랜딩: 오픽톡닥 → 하루오픽 (P-5)
> Phase 3 전체 완료(Step 4 튜터링까지) 후 진행. 도메인/코드/로고 일괄 변경.

| # | 작업 | 상세 |
|---|------|------|
| 1 | 도메인 구매 | haruopic.com 구매 |
| 2 | DNS 설정 | Vercel에 haruopic.com 연결 (A레코드 + CNAME) |
| 3 | 기존 도메인 리다이렉트 | opictalkdoc.com → haruopic.com (301), PG사 심사 완료까지 유지 |
| 4 | 코드 텍스트 치환 | "오픽톡닥" → "하루오픽", "OPIcTalkDoc" → "HaruOPIc" 전체 치환 |
| 5 | 로고 재생성 | 반창고 아이콘 + "하루오픽" Jua 폰트 (temp/generate-logo.html 활용) |
| 6 | CORS 변경 | T-4: haruopic.com **추가** (opictalkdoc.com도 당분간 유지) |
| 7 | 환경변수 | NEXT_PUBLIC_SITE_URL → haruopic.com |
| 8 | GitHub 저장소명 | opictalkdoc-app → haruopic-app (선택) |
| 9 | 문서 업데이트 | CLAUDE.md, 실행계획.md, 의사결정.md 내 이름/도메인 일괄 갱신 |

#### ✅ D-1: master_questions → questions 전환 완료
> `master_questions`(510행)를 `questions`(471행)로 전면 교체 완료.
> 13컬럼 구조, 새 ID 체계(`FRN_GEN_COM_DESC_01`), question_type 10종.
> 마이그레이션: `009_questions.sql`, 기존 모듈 FK 참조 전환 완료.

→ 상세: `docs/의사결정.md` D-1

#### Step 3: 모의고사 이관
1. **소리담 모의고사 소스코드 파악** (Edge Functions, 컴포넌트, DB, 타입, 로직)
2. **설계 문서 보강**: 소리담 원본 구현 섹션 추가 (`설계/모의고사.md`)
3. **DB 마이그레이션**: 5테이블 SQL 작성 + 실행
4. **Server Actions**: 세션 생성, 답변 제출, 상태 조회, 이력 관리
5. **Edge Functions**: Whisper STT + Azure 발음 + GPT 체크박스 + 규칙엔진 리포트
6. **UI**: 15문제 세션 + 실시간 진행 + 결과 리포트

---

*최종 업데이트: 2026-03-01*
*상태: Phase 3 Step 2 ✅ + D-1 DB 교체 ✅ → Step 3 모의고사*
